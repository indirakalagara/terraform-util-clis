id: github.com/cloud-native-toolkit/terraform-util-clis
name: util-clis
type: terraform
description: Module to install clis into local bin directory
tags:
  - tools
  - util
  - cli
  - yq
  - jq
  - igc
versions:
  - platforms: []
    dependencies: []
    variables: [{name: bin_dir, type: string, description: The directory where the clis should be downloaded. If not provided will default to ./bin, default: "", optional: true}]
    version: v1.0.2
    outputs:
      - name: bin_dir
        description: Directory where the clis were downloaded
  - platforms: []
    dependencies: []
    variables: [{name: bin_dir, type: string, description: The directory where the clis should be downloaded. If not provided will default to ./bin, default: "", optional: true}]
    version: v1.0.1
    outputs:
      - name: bin_dir
        description: Directory where the clis were downloaded
  - platforms: []
    dependencies: []
    variables: [{name: bin_dir, type: string, description: The directory where the clis should be downloaded. If not provided will default to ./bin, default: "", optional: true}]
    version: v1.0.0
    outputs:
      - name: bin_dir
        description: Directory where the clis were downloaded
  - platforms:
      - kubernetes
      - ocp3
      - ocp4
    dependencies: []
    #    - id: cluster
    #      refs:
    #        - source: github.com/ibm-garage-cloud/terraform-ibm-container-platform
    #          version: ">= 1.7.0"
    #        - source: github.com/ibm-garage-cloud/terraform-k8s-ocp-cluster
    #          version: ">= 2.0.0"
    #    - id: namespace
    #      refs:
    #        - source: github.com/ibm-garage-cloud/terraform-k8s-namespace
    #          version: ">= 2.1.0"
    #    - id: dashboard
    #      refs:
    #        - source: github.com/ibm-garage-cloud/terraform-k8s-dashboard
    #          version: ">= 1.6.0"
    variables: []
    version: v0.0.0
